# Final QC report — deep-time orientation (final targeted revision, 2026-09-25)

## 1. Baseline state
- Working tree started from merged #507 (`c20d28ea`), no unrelated
  uncommitted changes at start of the run.
- Canonical implementation: post-#497 decoder (stored-precision +
  stored-edge checks) plus post-#502 edge-check fixes and package wiring.

## 2. Tests run
- `pytest tests/` : 14/14 PASS (includes the two new direct_axis tests:
  `test_direct_axis_landmark_works_short_horizon`,
  `test_direct_axis_fails_when_landmark_lost`).
- `make all` clean rebuild: ran end-to-end — tests -> simulate ->
  summarize -> figures -> tables -> manuscript-results -> package ->
  qc_audit — with PASS 0 issues.

## 3. E11 / M1b rerun status
- New benchmark implemented minimally: `simulation.run_replication(...,
  direct_axis=True)` sets `N0 = Q[0]` at encode; decode returns the
  landmark's observed direction directly; landmark loss -> explicit
  `landmark_lost` failure.
- E11 grid (E11|M1b over T = 1e3..1e8, n_mc = 2000/cell; plus
  M1b_subst0.5 at t = 1e4..1e6, p_mismatch = 0.5) regenerated in
  `results/e11_m1b.csv` / `results/summary_e11.csv`.
- Results: P(err<1°) = 0.5045 at 1e3, 0.483 at 1e4 (capped ~0.5 by the
  unresolved mirror ambiguity — a single direction carries no handedness
  channel), 0.0265 at 1e5, 0 at >=1e6; invalid fraction 0.966 at 1e6,
  1.0 at >=1e7; substitution (pm = 0.5) degrades P to 0.247 at 1e4.
- Interpretation confirmed empirically: a direct-axis single landmark is
  algebraically valid while alive but fragile — no mirror resolution, no
  substitution buffer, no loss tolerance. Claim scope limited to the
  relational-scalar encoding as instructed.

## 4. H2 heterogeneity claim — corrected status
- E1c (unchanged headline): M5_quasars_50 P(<1°) = 1.0 vs M7_hybrid_all
  0.9711 at 1e7 yr; 0.043 vs 0.0138 at 1e8 yr.
- E7 grid search: best baseline allocation at 1e7 yr is quasar 20 only
  (P = 1.0) — verified against regenerated `summary_e7.csv`.
- H2 therefore `not_supported_baseline`; see
  `qc/hypothesis_status_final.csv` (H1-H6). Manuscript (Abstract, §3.8,
  §4.2, Conclusion), highlights, cover letter, supplementary, and
  FINAL_HANDOFF.txt revised consistently to "source-class
  stability/usability allocation dominates; heterogeneity helps only
  where it adds complementary failure resilience".

## 5. Numerical traceability
- `manuscript/values.json` regenerated: 101 keys incl. `m1b_*`,
  `m1b_subst_*`.
- `qc/numerical_traceability.csv`: all placeholders resolve `ok` to
  `results/summary_*.csv` via `scripts/manuscript_values.py`.
- All quoted numbers (Abstract, §3, tables) are placeholder-driven; no
  hard-coded results in `main.md`.

## 6. Figure/table audit
- Figures 1-7 cited in order of first appearance; Table 1 now includes
  the M1b row (generated by `make_tables.py`, not hand-edited).
- Captions/text agree with regenerated outputs; Abstract/Results/
  Discussion mutually consistent (qc_audit PASS).

## 7. Reference audit
- `qc/reference_audit.csv`: 18 references, all verified metadata,
  Vancouver numbering in order of appearance. No invented references.

## 8. Hostile internal reviewer/editor audit
- Info-cost claims: §3.7 reports the honest measured statement —
  precision axis flat at 1e7 yr (drift dominates); 8-bit degradation
  evidenced only at short horizon via E6c (0.04055° vs 0.0016° median
  at 1e4 yr). No claim rests on nominal bit counts alone.
- Stored-edge claims: substitution helps (0.8135 -> 0.835), injection
  neutral (1.0 -> 1.0), permutation impossible (0.9955 vs 0.9965) — all
  stated with the chirality-stored controls (E6b `b_chir`/`knn_chir`
  labels).
- Mismatch robustness claims capped at measured grids (RANSAC 0.9135 at
  pm = 0.5; failure at pessimistic+pm>=0.3 in E3/Fig 7).
- Chirality/mirror equivalence re-verified: sign-inversion check on the
  unmirrored frame is mathematically identical to checking the mirrored
  frame (documented in FINAL_POSTMERGE_QC.md); wrong-mirror fraction 0
  with chirality vs ~0.49 without.
- Single-landmark claim qualified: insufficiency stated for "a single
  generic reference source carrying only the proposed source–north
  relational scalar"; M1b benchmark disambiguates the general case.
- No invented astrophysical survival probabilities: usability/drift
  parameters remain declared scenario assumptions (Table 2), varied on
  sensitivity grids.
- Deep-time predictability: conclusions bounded to "under the baseline
  scenario / declared scenario assumptions".

## 9. Changes to manuscript conclusions
- H2 moved from "supported" to "not supported under baseline"; reframed
  as a nontrivial negative result, not a failure.
- Single-landmark claim narrowed to the relational-scalar encoding;
  M1b added as direct-axis control.
- Recommendation (§4.2) rewritten: allocate by assumed class
  stability/usability first; heterogeneity only where complementary.

## 10. Unresolved limitations (declared)
- Usability/drift/mirror-prior parameters are scenario assumptions, not
  fitted (sensitivity grids bound their influence).
- Synthetic populations; catalogue data descriptive only.
- M1b has no handedness channel by construction (its ~0.5 cap is the
  expected mirror coin-flip, not noise).
- Author-side fields outstanding: see `FINAL_USER_FIELDS_REQUIRED.txt`
  (affiliation, contact, ORCID, funding, competing interests, CRediT,
  suggested reviewers, DOI).

## 11. Submission-ready files (all regenerated 2026-09-25)
- `manuscript/build/manuscript_final.md` / `.docx`
- `manuscript/build/manuscript_inline.docx`
- `manuscript/highlights.md`, `cover_letter.md`, `declarations.md`,
  `supplementary.md`, `ACTA_ASTRONAUTICA_GUIDE_AUDIT.md`,
  `references.bib`, `graphical_abstract.png`
- `figures/fig1`–`fig7`.png; `tables/table1`–`table4`.csv/.tex
- `submission_package/` + `ACTA_submission_package.zip`
- `qc/*` (this report, qc_report, numerical_traceability,
  reference_audit, reproducibility_report, internal_reviewer_pass,
  hypothesis_status_final)
- `FINAL_HANDOFF.txt`, `FINAL_USER_FIELDS_REQUIRED.txt`
